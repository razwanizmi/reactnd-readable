import React from "react";
import { Header } from "../";

const NotFound = () => {
  return (
    <Header
      title="Oh Noes"
      text="Looks like the page you're looking for doesn't exist ☹️"
      color="#f2dede"
    />
  );
};

export default NotFound;
